{
  "outputDirectory": "out/renderer",
  "buildCommand": "pnpm build:web",
  "framework": null,
  "installCommand": "corepack enable && corepack prepare pnpm@10.28.1 --activate && pnpm install --ignore-scripts"
}
